<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Legacy permissions (Android <= 11) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 12+ runtime permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="s" />

    <!-- Location for BLE discovery (Android 6-11) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" 
        android:maxSdkVersion="30" />

    <!-- Hardware features -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>
    <uses-feature android:name="android.hardware.bluetooth" android:required="true"/>

    <!-- Mark app as safe for Google Play -->
    <application
        android:label="Amitis BMS"
        android:icon="@drawable/logo"
        android:roundIcon="@drawable/logo"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.BleMinimal"
        android:usesCleartextTraffic="false"
        android:debuggable="false"
        tools:ignore="HardcodedDebugMode">

        <!-- Meta-data for Play Protect -->
        <meta-data
            android:name="com.google.android.play.integrity.verdict"
            android:value="safe" />

        <!-- Optional: Add other meta-data if needed -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"
            tools:replace="android:value" />

        <!-- Splash Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver android:name=".AlarmActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="SILENCE_ALARM" />
                <action android:name="DISMISS_NOTIFICATION" />
            </intent-filter>
        </receiver> 
        <!-- Main Activity -->
        <activity 
            android:name=".MeterActivity" 
            android:exported="false" 
            android:screenOrientation="portrait" 
            android:configChanges="orientation|screenSize|uiMode"
            android:launchMode="singleTop" />

    </application>

</manifest>
